Figure 2 [Figure2.png]

Run Figure2.ipynb to create the required input files.

To create Figure2.png, run EventPlot.sh. Requires the files specified above to be
in the same folder as the shell script and GMT>6 installed.

Be in gps conda environment
EventPlot.sh makes the current figure. 
Only can have the two folders 0_* and 1_* in the directory or else fails
